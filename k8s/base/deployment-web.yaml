apiVersion: apps/v1
kind: Deployment
metadata:
  name: manix-web
  labels:
    app.kubernetes.io/name: manix-web
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: manix-web
  template:
    metadata:
      labels:
        app.kubernetes.io/name: manix-web
    spec:
      containers:
        - name: web
          image: ghcr.io/your-org/manix-web:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: manix-config
            - secretRef:
                name: manix-secrets
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"


